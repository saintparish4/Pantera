# Server
PORT=8080

# Database (Option 1: Single connection string - Railway/Heroku style)
DATABASE_URL=postgresql://user:password@localhost:5432/pym_db

# Database (Option 2: Individual variables - for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=yourdbname 

# Environment
ENV=development